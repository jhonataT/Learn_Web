const raio = 5.6; 
const area = Math.PI * Math.pow(raio, 2); // Usando Math para calcular a multiplicação de pi com o raio ao quadrado;

console.log(area.toFixed(1)); // Imprime a area com uma casa decimal;
console.log(typeof Math); // Para efeito de curiosidade, o tipo de dado da Math é objeto;

    // FIM da aula.
